apiVersion: v1
kind: Service
metadata:
  name: ingestion
  namespace: workflowai
  labels:
    app: ingestion
spec:
  selector:
    app: ingestion
  ports:
    - protocol: TCP
      port: 8001
      targetPort: 8001
  type: ClusterIP
