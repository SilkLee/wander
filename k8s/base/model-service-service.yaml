apiVersion: v1
kind: Service
metadata:
  name: model-service
  namespace: workflowai
  labels:
    app: model-service
    component: service
spec:
  type: ClusterIP
  selector:
    app: model-service
  ports:
    - port: 8004
      targetPort: 8004
      name: http
