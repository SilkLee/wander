apiVersion: v1
kind: Service
metadata:
  name: agent-orchestrator
  namespace: workflowai
  labels:
    app: agent-orchestrator
    component: service
spec:
  type: ClusterIP
  selector:
    app: agent-orchestrator
  ports:
    - port: 8002
      targetPort: 8002
      name: http
